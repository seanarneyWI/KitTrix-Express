# Database configuration (via SSH tunnel on port 5433)
DATABASE_URL="postgresql://motioadmin:M0t10n4lys1s@localhost:5433/motioPGDB"

# Server configuration
PORT=3001
NODE_ENV=development

# API URL for frontend (development only - production uses same origin)
VITE_API_URL=http://localhost:3001